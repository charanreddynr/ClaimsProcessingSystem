spring.application.name=ClaimsProcessingSystem
# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/claims_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Charanreddy@98  # Replace with yours!

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server
server.port=8080

# Logging
logging.level.org.springframework=INFO
logging.level.com.charan=DEBUG